# Destiny 프로젝트 작업 지침

이 파일은 세션이 시작될 때마다 읽는 규칙이다. 진행 상황이 아니라 **변하지 않는 규칙**만 적는다.
진행 상황과 결정은 `docs/`의 문서가 정본이다.

## 1. 정본과 근거

- 규격의 정본은 저장소의 `docs/` 문서다. 채팅이나 다른 도구에서 붙여 넣은 문서는 정본이 아니다.
  저장소 문서와 다른 내용을 받으면 구현하지 말고 어느 쪽이 정본인지 먼저 확인한다.
- 절기 시각, 역사적 사실(서머타임 구간, 표준시 변경일 등), 천문 상수를 기억이나 추정으로 채우지 않는다.
  근거는 저장소 문서, 측정 산출물, 사용자가 제공한 자료만 쓴다.
- 확인하지 못한 것은 "확인하지 못함"으로 보고한다. 추측을 사실처럼 쓰지 않는다.

## 2. 작업 범위와 승인

- 지시받은 단계(게이트)에서 멈추고, 사용자 승인 없이 다음 단계로 넘어가지 않는다.
- 지시에 없는 파일을 만들거나 고치지 않는다. 수정이 필요해 보이면 멈추고 이유를 보고한다.
- 다음 파일은 명시적 승인 없이 수정하지 않는다.
  - `src/destiny_saju/data_registry.py`, `solar_terms.py`, `calculator/timeline.py`
  - 기존 테스트 파일 (`tests/` 안의 기존 파일)
  - `src/destiny_saju/data/saju/solar_term_instants_v1.json`의 기존 범위(2026 등)
- 데이터셋의 `status`를 스스로 `production_verified`로 올리지 않는다. 승격은 사용자 승인 후에만 한다.
- 새 데이터는 기존 값을 덮어쓰지 않고 추가하는 방식으로만 넣는다.

## 3. Git

- `main`과 `fix/v3-data-integrity`에 직접 커밋하지 않는다. 자동화 러너가 쓰는 브랜치와 작업 브랜치를 분리한다.
- 커밋과 푸시는 사용자가 내용을 확인한 뒤에만 한다. 커밋 전에 `git status --short`와 `git diff --stat`를 보여 준다.
- 커밋 하나에는 한 가지 일만 담는다 (문서, 생성기, 데이터를 섞지 않는다).
- `tools/out/`은 `.gitignore` 대상이다. 커밋하지 않는다.
- 천문력 파일(`*.bsp`)과 개인 데이터(`data/private/`)를 커밋하지 않는다.

## 4. 보고 방식

- 보고에는 실제 실행한 명령과 출력을 요약하지 말고 그대로 넣는다.
- 작업이 끝나면 `pytest -v`의 마지막 요약 줄을 포함한다. 통과 개수는 실제 출력으로만 말한다.
- 긴 산출물(문서, 표)은 작성 즉시 파일로 저장하고 `cat`으로 출력한다. 연결이 끊겨도 잃지 않게 한다.
- 이전 수치나 결과를 인용할 때는 어느 파일 또는 어느 실행에서 나온 것인지 밝힌다.
  재현하지 못한 수치는 근거로 쓰지 않는다.

## 5. 테스트

- 실행 위치는 저장소 루트(`~/destiny`)다. 다른 디렉터리에서 실행하면 `import file mismatch`가 날 수 있다
  (자동화 러너의 작업 디렉터리 `actions-runner/_work/destiny/destiny`와 겹침).
- 기존 테스트가 실패하면 테스트를 고치지 말고 원인을 보고한다. 테스트 기대값을 바꿔서 통과시키지 않는다.
- 천문력 같은 외부 파일이 필요한 테스트는 파일이 없으면 skip 되어야 하고, 전체 실패로 이어지면 안 된다.

## 6. 비밀 값과 개인정보

- API 키, 서비스 키, 비밀번호를 파일, 커밋, 출력에 남기지 않는다. 환경변수(`KASI_SERVICE_KEY`,
  `ANTHROPIC_API_KEY` 등)로만 다루고 값을 출력하지 않는다.
- 사용자 출생 정보 등 개인 정보는 테스트 데이터와 로그에 실제 값을 쓰지 않는다.

## 7. 프로젝트 원칙

- 계산 기준은 투명하고 테스트 가능해야 한다. "절대적으로 정확하다"는 주장 대신,
  어느 범위까지 어떻게 검증되었는지를 데이터와 문서에 남긴다.
- 추측 금지: 불확실한 입력은 임의로 채우지 않고, 모호하면 후보로 분기하거나 fail-closed로 막는다.
- 외부 만세력 사이트에 런타임 의존하지 않는다.